---
import MainLayout from "@/layouts/MainLayout.astro";
import { DeviceDetailView } from "@/components/dashboard/DeviceDetailView";

const macParam = Astro.url.searchParams.get("mac") ?? "";
const pageTitle = macParam ? `Device • ${macParam}` : "Device Detail";
---

<MainLayout title={pageTitle}>
  <section class="mx-auto w-full max-w-[1100px] px-6 py-12">
    <DeviceDetailView mac={macParam} client:only="react" />
  </section>
</MainLayout>
